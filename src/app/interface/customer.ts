export interface Customer {
  name: string;
  email: string;
  region: string;
  country: string;
}

export interface Pin {
  title: string;
  image: string;
  collaborators: [];
  privacy: string;
}
